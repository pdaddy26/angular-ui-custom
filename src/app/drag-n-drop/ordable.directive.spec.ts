import { OrdableDirective } from './ordable.directive';

describe('OrdableDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdableDirective();
    expect(directive).toBeTruthy();
  });
});
